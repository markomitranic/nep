version: "2"

options:
  verbose: true
  max-attempt: 30
syncs:
  nep-mysql:
    src: './mysql/datastore'
    sync_strategy: 'native_osx'
    sync_excludes: ['.DS_Store']
  nep-project:
    src: './../public'
    sync_strategy: 'native_osx'
    sync_excludes:
      - './**/node_modules'
      - '.DS_Store'
      - '.git'
      - '.idea'
  nep-root:
    src: './../'
    sync_strategy: 'native_osx'
    sync_excludes:
      - './**/node_modules'
      - '.DS_Store'
      - '.git'
      - '.idea'
